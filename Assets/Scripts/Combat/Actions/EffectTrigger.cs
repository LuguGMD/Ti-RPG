using UnityEngine;

namespace RPG.Combat.Actions
{
    public enum EffectTrigger
    {
        ActionStart,
        ActionEnd,
        PatternEnd,
        BeforeTileStep,
        AfterTileStep,
    }
}
