using UnityEngine;

namespace RPG.Combat.Actions
{
    public enum EffectTriggerEnum
    {
        ActionStart,
        ActionEnd,
        PatternEnd,
        BeforeTileStep,
        AfterTileStep,
    }
}
